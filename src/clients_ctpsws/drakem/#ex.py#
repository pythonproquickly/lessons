    from collections import namedtuple

Score = namedtuple("Score", ["total", "soft_ace_count"])
score = Score(15, 1)
assert score.total == 15
assert score.soft_ace_count == 1

print(score.total)
score = score._replace(total=score.total + 10)
print(score.tot